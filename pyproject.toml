[project]
name = "juena-core"
version = "0.1.0"
description = "Shared agent/server/UI infrastructure extracted from juena-chatbot"
readme = "README.md"
authors = [
    { name = "Ahmad Z. Ihsan", email = "a.ihsan@fz-juelich.de" }
]
requires-python = ">=3.11, <4.0"
dependencies = [
    # Bounds below are the versions validated together in CP0b.
    # Re-validate and move them deliberately; do not widen them to "make it install".
    "langchain[openai]>=1.4,<2",
    "langgraph>=1.2,<2",
    "deepagents>=0.7.13,<0.8",
    "fastapi",
    "uvicorn[standard]",
    "sse-starlette",
    "httpx",
    "httpx-sse",
    "pydantic",
    "typing-extensions>=4.12",
    "langgraph-checkpoint-postgres",
    "psycopg[binary,pool]",
    "sqlalchemy[asyncio]",
    "pyyaml",
    "pillow",
]

[project.optional-dependencies]
ui = ["streamlit>=1.53.0"]
mcp = ["langchain[mcp]>=1.4,<2"]  # built-in; the standalone adapter is retired
sandbox = [
    "podman>=5.8,<6",
    "requests>=2.32,<3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[dependency-groups]
dev = [
    "fastmcp>=4.0.3,<5",
    "podman>=5.8,<6",
    "pytest>=9.1.1,<10",
    "pytest-asyncio>=1.4.0,<2",
    "requests>=2.32,<3",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
# The same guard juena-chatbot has carried since before the extraction. A
# LangChain deprecation warning is how the framework announces that something
# here will stop working at the next major version; as a warning it scrolls
# past in a green run, and both applications then inherit the breakage from
# core at once. Raised here so it fails in the package that owns the call.
filterwarnings = [
    "error::langchain_core._api.deprecation.LangChainDeprecationWarning",
]
